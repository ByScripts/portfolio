<template>
  <RouterView />
</template>

<!-- <style lang="postcss" scoped>
.main {
  max-width: var(--max-width);
  margin: 0 auto;
  padding: var(--section-padding);
}

/* Target only direct Markdown-rendered elements, not content inside Vue components */
.content-wrapper :deep(> div > div > h2) {
  font-size: 1.75rem;
  margin-bottom: 1.5rem;
  margin-top: 3rem;
  color: var(--color-text);
  position: relative;
  display: inline-block;
}

.content-wrapper :deep(> div > div > h2:first-child) {
  margin-top: 0;
}

.content-wrapper :deep(> div > div > h2::after) {
  content: "";
  position: absolute;
  bottom: -6px;
  left: 0;
  width: 40px;
  height: 2px;
  background: var(--color-accent-warm);
  border-radius: 1px;
}

.content-wrapper :deep(> div > div > h3) {
  font-size: 1.125rem;
  font-weight: 600;
  margin-top: 2rem;
  margin-bottom: 1rem;
  color: var(--color-text);
}

.content-wrapper :deep(> div > div > p) {
  color: var(--color-text-light);
  margin-bottom: 1rem;
}
</style> -->
